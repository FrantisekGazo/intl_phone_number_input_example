name: myapp
description: A new Flutter application.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl_phone_number_input: 0.4.6

dev_dependencies:
  # unit testing
  test: 1.14.3
  # integration testing
  flutter_driver:
    sdk: flutter

flutter:
  uses-material-design: true
